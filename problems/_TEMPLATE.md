---
id: kebab-case-unique-id
title: Short Problem Title
category: must-match-folder-name
difficulty: easy | medium | hard
trigger: "one-line cue you should recognize in the statement"
pattern: "the technique that cue should fire (e.g. sort + sweep)"
time_target_min: 15
---

<!--
RULES for adding a problem (중요):
- 원문 문제를 그대로 붙여넣지 말 것. 다른 플랫폼(CodeSignal 등)에서 푼 문제는
  "무엇을 묻는가"만 유지하고 스토리/이름/수치/예시를 전부 바꿔서 재작성한다.
- 바꾼 예시는 반드시 솔루션 코드를 실제로 돌려서 출력을 검증한 뒤 적는다.
- Statement(Problem/Examples/Constraints)는 영어(미국 영어) — 실전처럼 영문
  스펙 독해를 훈련하기 위함. Approach 이하 노트는 한국어 허용.
- "## Approach" 위쪽이 타이머 중에 보이는 부분, 아래쪽이 Reveal 후 보이는 부분.
  이 구분 때문에 "## Approach" 헤딩은 필수다.
- category는 폴더 이름과 일치해야 한다 (로더가 검증함).
-->

## Problem

Rewritten statement in US English. State the input, the required output, and
every rule precisely - this section is what the timer runs against.

## Examples

Input / output pairs, recomputed for the rewritten values.

## Constraints

- Input size bounds, value ranges, target complexity if stated.

## Approach

핵심 아이디어. 왜 이 패턴인지, 브루트포스는 왜 안 되는지.

## Complexity

- 시간: O(...)
- 공간: O(...)

## Solution (Python)

```python
# code
```

## Solution (Java)

```java
// code
```

## Verification

예시 입력으로 실제 실행해 확인한 내용.
